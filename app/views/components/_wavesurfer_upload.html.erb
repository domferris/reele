    <div class="wave-container">
      <div class="wave-section">

        <div class="wave-controls">
          <i id="waveform-play" class="fas fa-play-circle"></i>
        </div>

        <div id="waveform" class="waveform-player" data-audio=<%= @project.video_url%>>
        </div>

      </div>

      <%= image_tag(@project.photo, class:"wave-image") if @project.photo %>
    </div>
