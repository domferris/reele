<h2><%= @user.email %></h2>

<%= link_to "+ New Project", new_project_path %>

<% if @user.projects.present? %>
<ul>
  <% @user.projects.each do |project| %>
  <li>
    <%= project.title %>
  </li>
  <% end %>
</ul>
<% end %>
