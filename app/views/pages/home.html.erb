<div class="banner" style="background-image: linear-gradient(rgba(175,64,53,1),rgba(175,64,53,0.8));">
  <div class="container">
    <h1>Discover the best talent in audio</h1>
    <p>Reele helps sound designers showcase their skills, find collaboration opportunities, and thrive in the industry.</p>
    <a class="btn btn-flat" href="#">Sign up</a>
  </div>
</div>
<div class="filter-bar">
  <!-- <input class="mr-sm-2" type="search" placeholder="Search" aria-label="Search">
  <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button> -->
    <h5 class="m-0">Filter by category:</h5>
    <%= link_to 'Mixing', root_path(query: 'Mixing') %>
    <%= link_to 'Mastering', root_path(query: 'Mastering') %>
    <%= link_to 'Audio Engineer', root_path(query: 'Audio Engineer') %>
    <%= link_to 'Producer', root_path(query: 'Producer') %>
    <%= link_to 'Sound Design', root_path(query: 'Sound Design') %>
    <%= link_to 'Composition', root_path(query: 'Composition') %>
</div>
<div class="container mt-4 mb-4">
  <div class="cards">
    <%= render 'components/card' %>
    <!-- WE WILL NEED THIS TO ITERATE OVER PROJECTS -->
    <%# @projects.each do |project| %>
      <%#= render 'components/card', project: project %>
    <%# end %>
  </div>
</div>

